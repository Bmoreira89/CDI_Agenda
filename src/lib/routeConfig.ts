// src/lib/routeConfig.ts
// Estes exports têm que ser PRIMITIVOS (número / string), não objetos.
export const dynamic = 'force-dynamic';
export const revalidate = 0;
export const fetchCache = 'force-no-store';
